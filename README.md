# IntroToDLSegmentation
 
### Run the model:

As the checkpoint is already saved, you can run the model by running the following command:
    
```
python infer.py -c <checkpoint_path> -i <input_path> -d <device>
```